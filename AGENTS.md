<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Graverse Lab Repository Guide

- Brand: Graverse Lab (Graph + Universe)
- Descriptor: Graph Theory, Algorithms & Network Science
- Principal investigator: Gülnaz Boruzanlı Ekinci
- English is canonical; Turkish is a reviewed translation.
- Use structured repository content and GitHub pull requests.
- Never invent people, projects, grants, metrics, or publication metadata.
- Keep static export compatibility until hosting is finalized.
- Interactive graph features require semantic equivalents, keyboard access, and reduced motion.
- Do not activate forms, analytics, or newsletter collection without approved providers and privacy text.
- Preserve the legacy root `index.html` until the replacement is explicitly approved.
- Verify with `npm run typecheck` and `npm run build`.
